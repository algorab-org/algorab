name: Setup repo, Java, caches & Mill

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: 21

    - name: Cache Mill & Coursier directories
      uses: actions/cache@v4
      with:
        path: |
          ~/.mill
          ~/.coursier
          ~/.cache/coursier
          ~/.cache/cs
        key: ${{ runner.os }}-${{ hashFiles('build.mill') }}
        restore-keys: |
          ${{ runner.os }}-