class Counter =
  mut val x = 0

  def increment(): Int =
    x = x + 1
    5 -- TODO remove

  this -- TODO remove

val counter = Counter()
val counter2 = Counter()
println(counter.x)
println(counter2.x)
counter.increment()
println(counter.x)
println(counter2.x)